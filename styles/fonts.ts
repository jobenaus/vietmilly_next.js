import { Nerko_One } from '@next/font/google';
import { Fuzzy_Bubbles } from '@next/font/google';

export const nerko_one = Nerko_One({ weight: '400' });
export const fuzzy_bubbles = Fuzzy_Bubbles({ weight: '400' });
